version: "3"

tasks:
  clean:
    summary: Remove build artifacts (src-tauri/target, .generated). Destructive — do not run without authorization.
    cmds:
      - rm -rf {{.TAURI_DIR}}/target {{.GENERATED_DIR}}
