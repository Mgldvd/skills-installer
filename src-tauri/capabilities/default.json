{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions required by the main window, including native folder selection for the installation destination.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:default", "core:event:default", "dialog:allow-open"]
}
